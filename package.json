{
  "name": "final-project",
  "version": "1.0.0",
  "description": "Final project for CI/CD pipeline",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo 'No tests' && exit 0",
    "lint": "echo 'No lint' && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
